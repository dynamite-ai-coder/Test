services:
  - type: web
    name: OpenBullet2
    runtime: docker
    plan: free
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
    healthCheckPath: /
    autoDeploy: true
