Niestety nie ma możliwości szukania wielu domen jednocześnie itd ale chcę stworzyć bota na telegram który będzie się komunikował przez API z linku z botem tmlogs. Chce wpisać w mojego bota np examplelink1.com
examplelink2.com itd
I chciałbym aby mój bot komunikując się z tmlogbot wykonał /search examplelink1.com następnie bot zapyta URL:login:pass czy login:pass. Niech mój bot na sztywno wybiera login:pass itd a następnie po sprawdzeniu wszystkich plików niech przesyła mi każdy plik txt z login:pass i niech każdy plik nazywa nazwadomeny
link do API info tmlogbot 
apikey mojego bota telegram 8997670334:AAGezPaGSCl5RvxDAX-X86uKUjq1GoDr6eg
chciałbym jeszcze dodać żeby nazywał pliki nazwa domeny szukanych oraz niech mój bot zwraca także info o ilości lini w szukanych plikach 

promt 
You are working on my GitHub repository:

https://github.com/dynamite-ai-coder/Test

First, inspect the repository and read "new.txt" completely.

I want to build a Telegram bot in Python that accepts multiple domains from the user, for example:

examplelink1.com
examplelink2.com
examplelink3.com

The bot should process each domain independently and communicate with the documented API/service described in "new.txt".

Requirements:

1. Read and understand the existing "new.txt" documentation before making changes.
2. Implement a clean Python Telegram bot architecture.
3. Accept multiple domains in one Telegram message, one domain per line.
4. Process domains sequentially or through a controlled async queue.
5. For each domain, perform the corresponding API search operation.
6. Handle API pagination, rate limits, timeouts, errors and retries properly.
7. Do not expose, collect, store, or forward passwords, authentication cookies, session tokens, or other credentials.
8. Results must contain only safe metadata such as:
   - domain
   - URL where appropriate
   - record count
   - status
   - timestamps
   - non-sensitive record identifiers
9. Generate one ".txt" report per domain using the domain as the filename, for example:
   "examplelink1.com.txt"
10. Send each generated report back to the requesting Telegram user.
11. Sanitize filenames and Telegram output to prevent path traversal and injection.
12. Never log API keys, Telegram bot tokens, passwords, or other secrets.
13. Load secrets from environment variables or a ".env" file and provide a ".env.example".
14. Add clear configuration instructions and a "requirements.txt".
15. Keep the implementation compatible with the current versions of the libraries used.
16. Add robust exception handling and useful logging without sensitive data.
17. Do not implement functionality for extracting, exporting, or transmitting "login:pass" credentials.

Before coding, inspect the entire repository structure and determine whether existing code should be reused or replaced.

Then implement the project completely.

After implementation:

- run syntax checks/tests,
- fix all errors,
- show me the final project structure,
- explain how to configure the Telegram bot token and API credentials,
- provide the exact command to install dependencies,
- provide the exact command to run the bot.